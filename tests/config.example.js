/* eslint-disable max-len */
const config = {
    // location, where your binary is
    webDriverPath: '.../bin/webdrivers/osx_64/chromedriver',
    // ui URL
    targetBaseUrl: 'http://localhost:3000/',
    // multiplier for forced awaits
    speed: 1.0
};

export { config };
